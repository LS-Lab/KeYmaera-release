\sorts{
R;
}

\functions {
R b;
R amax;
R ep;
}

\problem {
\[ R vdes, SB, mo, t, a, v, z, m, d, do \] ((  v^2 - d^2 <= 2*b*(m-z) & v >=0 & ep > 0 & b > 0 & amax > 0 & d >= 0)
-> 
		\forall R p; \<(
		(do:= d; mo:=m; m := *; d := *; ?d >= 0)
		/*(do:= d; mo:=m; m := *; d := *; ?d >= 0 & do^2 - d^2 <= 2*b*(m-mo))*/
		) \>

	\<( 
	?v^2 - d^2 <= 2*b*(m-z);
SB := ((amax/b)+1)*ep*v+((v^2 - d^2)/(2*b)) + ((amax/b)+1)*amax*ep^2/2;
		a:=amax;
		((?m - z <= SB; a:= -b)
		++ ?m - z >= SB);
		(t:=0; {z`=v, v` = a, t`=1, (v >= 0 & t <= ep)}))*
\> 
(z >= p))
}
