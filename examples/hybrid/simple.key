/**
 * Simple example of a hybrid system for KeYmaera.
 * Point x is moving on a line towards 0.
 */

\problem {
  \[ R x, a, t, c; \] (
    (x^2<(4*c)^2)
  ->
   \[(
      if (x>0) then
        a := -4
      else
        a := 4
      fi;
      t:=0;
      {x`=a,t`=1, t<=c}
    )@invariant(x^2<=(4*c)^2)*\] (x^2<=(4*c)^2)
  )
}
