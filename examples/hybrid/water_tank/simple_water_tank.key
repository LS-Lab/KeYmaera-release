/**
 * @provable
 */
\problem {
  \[R x, q\] (
    x<=9 ->
   \[q:=1;
     ( (?q=1; {x'=1,x<=9})
     ++(?q=1&x>=5; x:=x-1; q:=0)
     ++(?q=0; {x'=-1})
     ++(?q=0&x<=2; q:=1; ?x<=9))*
     @invariant(x<=9)
   \] x<=9
  )
  
}