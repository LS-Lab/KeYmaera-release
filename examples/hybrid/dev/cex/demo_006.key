/**
 * Tests the effect of assignments to quantified variable
 *
 * once a variable becomes quantified, it could not be again used in a program block,
 * meaning, it could not be updated as one would update a program variable. hence, it
 * seems reasonable
 */

\problem {
    \forall R x;
    (
     \[
        R x;
        x:=1
     \] (
         x=1
        )
    )
}
