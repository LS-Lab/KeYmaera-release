/**
 * examples involving multiple states before entering the first-order differential system
 *
 * special note:
 * in the new code that uses GSP, if we reverse the order of x:=-x and x:=x, would never have left
 */

\problem {
    \[ R x, y; x:=10; (x:=-x ++ x:=x); {x'=y, x<10} \] (x>-10)
}
