/**
 * tests the ++ functionality
 *
 * expected: x=0 should be the counterexample
 */

\problem {
    \[
        R x;
        x:=1;
        x:=x*2 ++ (x:=x*2+3 ++ (x:=x+1 ++ (x:=4*x-3 ++ x:=-1+1)))
    \] (x>0)
}
