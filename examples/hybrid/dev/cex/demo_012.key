/**
 * makes use of the ?(\alpha) program notation
 *
 * w/ the x!=13 condition, there should have been no counter-example
 * if we change x!=13 to x=13, then there would have been a counter-example
 */

\problem {
    \[
        R x;
        x:=13;
        ?(x=13);
        x:=-13
    \] (x>0
       )
}
