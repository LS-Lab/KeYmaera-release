/**
 * an even more complicated example involving if statement and ++
 *
 * if we don't comment out x=-100, no cex
 * o.w. cex x=-100
 */

\problem {
    \[
        R x; x:=3; if (x>0) then x:=-x fi; if (x>0) then x:=2*x else x:=x-1 fi; (x:=30-x ++ x:=2*x-1); (x:=-3*x+2 ++ x:=4*x-3);
    \] (x=-100 | x=133 | x=29 | x=-39)
}
