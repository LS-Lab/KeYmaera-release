/**
 * A single car accelerates at constant rate a > 0.
 * 
 * Variable Definitions:
 * x position of car
 * v velocity of car
 * a acceleration of car
 * A max acceleration 
 * B max braking 
 * StopSign position of Stop Sign
 */

\problem{
  \[ R x, v, a; R B, A, StopSign \] (
    (  v >= 0  
     & A  > 0 )
->
  \[{x'' = A}\] (v >= 0)
  )
}

