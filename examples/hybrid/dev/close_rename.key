\sorts {
  C;
}

\functions {
  R x(C);
  \nonRigid[Location] C next(C);
  C first;
}

\programVariables {
  C i,j;
}

\problem {
\[ i := first; (i := next(i))*\] (x(i) = 1)
-> \[ j := first; (j := next(j))* \] (x(j) = 1)
}