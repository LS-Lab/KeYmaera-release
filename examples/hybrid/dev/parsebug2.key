/* KeYmaera should not parse this.
 */
\functions {
  R ep;
  R b;
}

\problem {
  \[ R x1,v1,a1, t; R A; A:=1 \] (
    b>0 & A>=0 & ep>0
  -> 
   \[(
        a1 := A);
    (t:=0;{x1'=v1,v1'=a1, t'=1, (v1>=0});
    )*
   \] (x1<5)
  )
}
