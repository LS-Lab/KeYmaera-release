/**
 * @bug shouldn't be provable by diffreplace_box_right
 * @todo needs to check the set of changed variables.
 * @false
 */
\programVariables {
  R x,y;
}

\problem {
  \[{x'=x*y}\]x>0 -> \[{x'=x*y,y'=-x}\]x>0
}