/**
 * Hybrid bouncing ball example.
 * h = height
 * v = velocity
 * H = height limit
 * g = gravitation
 * c = elastic dampening factor at floor (h=0)
 * not sufficient: @invariant(0<=h&h<=H&v^2<=2/3*(H-h)*g)
 */

\problem {
  \[ R h,v; R c,g,H,V; \] (
    0<=h&h<=H &v=0 & g>0 & 0<=c&c<1
  ->
   \[(
      if (h=0) then
        v := -c*v
      fi;
      {h`=v,v`=-g, h>=0}
    )*\] (0<=h&h<=H)
  )
}
