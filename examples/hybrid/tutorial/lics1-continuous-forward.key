\programVariables {
	R x, v, a;
}

/**
 * @provable
 * A single car accelerates at constant rate a > 0.
 * 
 * Variable Definitions:
 * x position of car
 * v velocity of car
 * a acceleration of car
 */
\problem{
  (  v >= 0  
     & a >= 0 )
->
  \[{x' = v, v' = a}\] (v >= 0)
}

