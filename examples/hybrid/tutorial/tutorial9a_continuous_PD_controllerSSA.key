/* 
*
* Continuous-time controller to stabilize the car around a reference
* position r, with velocity zero. 
*
* Should prove automatically
*
* Author: Nikos Arechiga
*/

\problem{

\[R p, v, a, S, Kp, Kd, c, r \] (

	( 
		v >= 0
		& Kp = 2 & Kd = 3
		
		& c > 0
		& (5/4)*(p - r)^2 + ((p-r)*v)/2 + v^2/4 < c

	) ->

\[(

	{
		p' = v, 
		v' = -Kp*( p - r ) - Kd*v

		& v >= 0 
	}


 	)
 	\] ( 
 		(5/4)*(p-r)^2 + ((p-r)*v)/2 + v^2/4 < c
	)

)
}
