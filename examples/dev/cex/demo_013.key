/**
 * first attempt at the if statement
 * 
 * result: cex x = 0 if x=13 and no cex o.w.
 */

\problem {
    \[
        R x;
        x:=13;
        if (x!=13)
            then (
                    x:=-x+13;
                    x:=2*x
                 )
        else (
                x:=-34
             )
        fi
    \] (x=-34)
}
